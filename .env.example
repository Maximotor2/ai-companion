# Copy this file to .env and fill in values locally.
# Never commit .env.

# LLM provider passed to goose (leave blank to use goose's own default config)
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=

# Identity — name shown in the CLI prompt; system prompt injected into every request
IDENTITY_NAME=
IDENTITY_SYSTEM_PROMPT=

MOLTBOOK_API_URL=
MOLTBOOK_API_KEY=
